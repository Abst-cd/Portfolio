---
import { Image } from 'astro:assets'
import ajolotemc from '../assets/images/ajolotemc.jpg'
---

<h1 class="titulo">Software Engineer</h1>
<Image src={ajolotemc} alt="Ajolote" width={500} height={400} class="ajolote" />
<style>
    .titulo {
        color: rgb(119, 119, 133);
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        text-align: center;
        margin-top: 50px;
    
    }   
    .aboutPerson {
         font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        text-align: center;
        font-size: 18px;
        color: #ffffff;
        margin: 20px auto;
        max-width: 760px;
        line-height: 1.6;
        letter-spacing: 0.2px;
        padding: 14px 18px;
        border-radius: 10px;
        background: linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.04));
        box-shadow: 0 8px 20px rgba(0,0,0,0.25);
        backdrop-filter: blur(4px);
        transition: transform .22s ease, box-shadow .22s ease;
        animation: fadeUp .6s ease both;
        display: block;
    }
    .intro {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        text-align: center;
        font-size: 18px;
        color: #ffffff;
        margin: 20px auto;
        max-width: 760px;
        line-height: 1.6;
        letter-spacing: 0.2px;
        padding: 14px 18px;
        border-radius: 10px;
        background: linear-gradient(90deg, rgba(255,255,255,0.02), rgba(255,255,255,0.04));
        box-shadow: 0 8px 20px rgba(0,0,0,0.25);
        backdrop-filter: blur(4px);
        transition: transform .22s ease, box-shadow .22s ease;
        animation: fadeUp .6s ease both;
        display: block;
    }
    .intro:hover {
        transform: translateY(-4px);
        box-shadow: 0 14px 34px rgba(0,0,0,0.32);
    }

    @keyframes fadeUp {
        from { opacity: 0; transform: translateY(10px); }
        to   { opacity: 1; transform: translateY(0); }
    }
    .ajolote {
        display: block;
        margin: 16px auto;
        border-radius: 12px;
    }
</style>
<p class="aboutPerson">19 years old from Mexico.</p>
</p>
<p class="intro">Passionate about building scalable web applications.</p>
<p class="intro">In my free time, I enjoy hitting the gym, goind outside, and exploring new tech trends.</p>
<hr>
